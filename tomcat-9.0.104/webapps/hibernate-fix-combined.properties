# Combined Hibernate configuration for both admin and mycompany applications
# hbm2ddl.auto 속성을 validate로 설정하여 스키마 검증

# For both admin and mycompany (validates existing tables)
blPU.hibernate.hbm2ddl.auto=validate
blCMSStorage.hibernate.hbm2ddl.auto=validate
blSecurePU.hibernate.hbm2ddl.auto=validate
